"""
Garde-fous A/B (placeholder).
Étape 1: validations no-op pour permettre l'import sans erreurs.
"""
def assert_flag_defined(flag: str) -> None:
    return

def assert_variant_has_component(page: str, slot: str, variant: str) -> None:
    return

def flag_rollout_bounds(flag: str) -> None:
    return
import logging
log_api = logging.getLogger("leads.api")
log_tasks = logging.getLogger("leads.tasks")
log_enrich = logging.getLogger("leads.enrich")
log_route = logging.getLogger("leads.route")
log_antispam = logging.getLogger("leads.antispam")
log_submit = logging.getLogger("leads.submit")  # ⬅️ nouveau
